openapi: 3.1.0
info:
  title: Hanzo Cloud
  version: 1.0.0
  summary: Discovery index for the Hanzo Cloud API surface.
  description: |
    Hanzo Cloud is the unified API surface for the Hanzo AI Platform. Every
    service is reachable through a single gateway at `api.hanzo.ai` with a
    single bearer token issued by Hanzo IAM (`https://hanzo.id`).

    This document is the discovery index. Each service has its own complete
    OpenAPI 3.1 specification under `openapi/<service>/openapi.yaml`. There
    are no cross-file `$ref` links between service specs — each is
    self-contained.

    Authentication: Bearer JWT issued by Hanzo IAM. OIDC discovery is at
    `https://hanzo.id/.well-known/openid-configuration`.

    Routing: every service is exposed at `https://api.hanzo.ai/v1/<service>/*`
    in addition to its own subdomain. Every Hanzo route lives under `/v1/<service>/`.
    No legacy prefixes exist anywhere in the Hanzo HTTP contract.
  contact:
    name: Hanzo AI
    url: https://hanzo.ai
    email: support@hanzo.ai
  license:
    name: Proprietary
    url: https://hanzo.ai/terms
servers:
- url: https://api.hanzo.ai
  description: Hanzo Gateway Production
security:
- BearerAuth: []
tags:
- name: Discovery
  description: Index of Hanzo services and routing.
- name: Health
  description: Liveness checks.
- name: OIDC
  description: OpenID Connect discovery.
paths:
  /v1/discovery:
    get:
      tags:
      - Discovery
      summary: List Hanzo services
      description: |
        Canonical index of Hanzo services with their subdomain, gateway route,
        and OpenAPI spec URL. NOTE: the machine-readable index is currently the
        set of per-service specs in this repository; the aggregated JSON endpoint
        at the gateway is not yet wired.
      operationId: listServices
      security: []
      responses:
        '200':
          description: List of services with subdomain, gateway route, and spec URL.
          content:
            application/json:
              schema:
                type: object
                required: [services]
                properties:
                  services:
                    type: array
                    items:
                      $ref: '#/components/schemas/Service'
              example:
                services:
                - name: cloud
                  subdomain: https://api.hanzo.ai
                  gateway_route: /v1/chat/completions
                  spec: https://raw.githubusercontent.com/hanzoai/openapi/main/cloud/openapi.yaml
                - name: iam
                  subdomain: https://hanzo.id
                  gateway_route: /v1/iam
                  spec: https://raw.githubusercontent.com/hanzoai/openapi/main/iam/openapi.yaml
  /v1/health:
    get:
      tags:
      - Health
      summary: Gateway health check
      operationId: gatewayHealth
      security: []
      responses:
        '200':
          description: Gateway is healthy.
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  msg:
                    type: string
                  data: {}
                  data2: {}
              example:
                status: ok
                msg: ''
                data: null
                data2: null
  /.well-known/openid-configuration:
    get:
      tags:
      - OIDC
      summary: OIDC discovery
      description: |
        OpenID Connect discovery document. Served by Hanzo IAM at its own host:
        https://hanzo.id/.well-known/openid-configuration. The advertised
        endpoints live under `/v1/iam/oauth/*` and `/v1/iam/.well-known/jwks`.
      operationId: oidcDiscovery
      security: []
      responses:
        '200':
          description: OIDC configuration document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  issuer: { type: string, format: uri }
                  authorization_endpoint: { type: string, format: uri }
                  token_endpoint: { type: string, format: uri }
                  userinfo_endpoint: { type: string, format: uri }
                  jwks_uri: { type: string, format: uri }
              example:
                issuer: https://hanzo.id
                authorization_endpoint: https://hanzo.id/v1/iam/oauth/authorize
                token_endpoint: https://hanzo.id/v1/iam/oauth/token
                userinfo_endpoint: https://hanzo.id/v1/iam/oauth/userinfo
                jwks_uri: https://hanzo.id/v1/iam/.well-known/jwks
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Bearer credential accepted on every `/v1/*` route. Either a JWT issued
        by Hanzo IAM (https://hanzo.id) or a Hanzo API key (`hk-...`). OIDC
        discovery: https://hanzo.id/.well-known/openid-configuration. Obtain a
        token via /v1/iam/auth/login or /v1/iam/oauth/token, or mint an API key
        in the console (console.hanzo.ai).
  schemas:
    Service:
      type: object
      required: [name, subdomain, gateway_route, spec]
      properties:
        name:
          type: string
          description: Service slug.
        subdomain:
          type: string
          format: uri
          description: Service's own canonical hostname.
        gateway_route:
          type: string
          description: Path prefix served through api.hanzo.ai.
        spec:
          type: string
          format: uri
          description: URL of the service's OpenAPI 3.1 spec.
