# ~7-line canonical caller — all real config lives in /hanzo.yml.
# Builds + pushes the agent-skills artifact on OUR arc pool; auto-mirrors to
# registry.hanzo.ai.
name: CI/CD
on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
jobs:
  cicd:
    uses: hanzoai/ci/.github/workflows/build.yml@v1
    secrets: inherit
